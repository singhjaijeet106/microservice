spring:
 application:
  name: config-server
 cloud:
  config:
   server:
    git:
     uri: https://github.com/singhjaijeet106/micro-config-server
     clone-on-start: true

server:
 port: 8050
# servlet: 
#   context-path: /config/
